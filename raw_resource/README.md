# raw_resource

Bản **HTML tĩnh gốc** của Votely (vanilla JS + Tailwind CDN).

- Chỉ dùng để **tham chiếu** khi so sánh hành vi hoặc migrate thủ công.
- **Không** được deploy — ứng dụng chạy từ `src/` (Vite + Vue) tại root repo.
